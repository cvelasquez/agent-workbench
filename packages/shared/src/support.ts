/**
 * Cómo apoyar el proyecto (§6.28): una sola dirección para la línea del
 * arranque, el cartel de la web y el enlace del diálogo de atajos. La del
 * paquete de npm (`funding`) sale del `package.json` de la raíz, y
 * `check-composer-input.mjs` comprueba que sea la misma.
 */

export const SUPPORT_URL = 'https://buymeacoffee.com/cvelasquez';

/**
 * Lo que Vite reemplaza al compilar (`define`, `vite.config.ts`): la versión
 * del `package.json` de la raíz, para el cartel de §6.28. En Node no existe.
 */
declare const __APP_VERSION__: string | undefined;

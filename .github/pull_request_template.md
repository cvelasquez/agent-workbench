## Why

<!-- The problem this solves, in two or three lines. -->

## What changes

-

## Checklist

- [ ] `pnpm typecheck`, `pnpm check` and `pnpm build` pass
- [ ] It respects the four hard rules in `ARCHITECTURE.md` (credentials, binaries, brand, local server)
- [ ] New UI texts go through `t()` and are in every locale file
- [ ] If it touches an area covered by a check, the new case is in that check

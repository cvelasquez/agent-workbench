<?xml version="1.0" encoding="utf-8"?>
<!--
  La app de Android de Agent Workbench (hito 38, §15): un túnel SSH hacia la PC
  del usuario, un visor con la interfaz que sirve esa PC y un vigía que avisa
  cuando un agente termina o espera.

  Permisos, uno por uno: red (el túnel), estado de la red (reconectar cuando
  vuelve el wifi), CHANGE_WIFI_STATE (lo exige el tipo connectedDevice del
  servicio), el servicio en primer plano y su tipo, avisos, y cámara (el QR, la
  pide el lector recién al escanear). Nada de ubicación, contactos ni archivos.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!--
      Sin respaldo: la llave SSH y la credencial están cifradas con una llave
      del almacén del teléfono, que no viaja en un respaldo; restaurarlas en
      otro teléfono no serviría, y copiarlas a la nube tampoco.
    -->
    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgentWorkbench"
        tools:targetApi="36">

        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|keyboard|uiMode|density|fontScale|navigation"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.PairingActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false" />

        <!-- El lector del QR: el de ZXing, apartado de las barras del sistema y girando
             con el teléfono (ScanActivity). El de la biblioteca, fijo en horizontal, no se usa. -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            tools:node="remove" />
        <activity
            android:name=".ui.ScanActivity"
            android:exported="false"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <service
            android:name=".tunnel.TunnelService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
